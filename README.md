# 🐕 DogSherlock

A mobile app for tracking your dog's activities, built with **Expo** and **React Native**.

## 🚀 Getting Started

### Prerequisites

- **Node.js**: `v18.x` or higher
- **Yarn**: `3.x.x` (via Corepack)
- **Expo CLI**: Bundled in project (no global install needed)

### 📥 Installation

1. **Clone the repository**:

   ```bash
   git clone git@github.com:equaleyes/canis-dog-tracker-fe.git
   cd canis-dog-tracker-fe
   ```

2. **Enable Corepack (if not already enabled)**:

   ```bash
   npm i -g corepack
   corepack enable
   ```

3. **Install dependencies**:

   ```bash
   yarn install
   ```

4. **Start development server**:

   ```bash
   yarn start
   ```
